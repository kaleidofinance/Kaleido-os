"use client";

// Export only the container which will dynamically load the widget
export { default as ChatbotContainer } from './ChatbotContainer';

// Export other utilities
export * from './config';
export * from './useChatbot';
