node_modules/
out/
.remotion/

# Generated by scripts/gen-audio.mjs, deterministically — so the 5.5MB WAV is a
# build artifact rather than a source file. `npm run render` regenerates it.
public/bed.wav
